# Contentful (prices page) — from the old api-server config
CONTENTFUL_SPACE_ID=
CONTENTFUL_ACCESS_TOKEN=

# Contact form email (Gmail + app password) — from the old api-server config
EMAIL_USER=
EMAIL_PASS=
EMAIL_TO=
